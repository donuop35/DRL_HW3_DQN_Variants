name: drl-hw3
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip>=23.0
  - pytorch>=2.0.0
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8  # Remove if CPU only
  - numpy>=1.24.0
  - matplotlib>=3.7.0
  - pandas>=2.0.0
  - pyyaml>=6.0
  - tqdm>=4.65.0
  - jupyter
  - ipykernel
  - pip:
    - pytorch-lightning>=2.0.0
    - tensorboard>=2.13.0
    - gymnasium>=0.29.0
